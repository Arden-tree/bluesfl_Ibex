You are a debugging assistant for a RISCV microprocessor design team.
Please look through the following Test Report and SystemVerilog Repository structure and provide a list of files that
one would root cause located in.

Please only provide the full path and return files.
The returned files should be in json format wrapped with ```
For example:

```json
{
  "suspicious_files": [
    "file1.sv",
    "file2.sv"
  ]
}
```